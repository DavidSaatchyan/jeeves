[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    eval: marks tests that require RAG eval pipeline (LLM calls + Chroma)
    classification: marks tests for intent classification audit
